# Linux 容器内执行或读取的文件必须保持 LF，避免 Windows 检出后出现 ^M 错误。
*.sh text eol=lf
**/*.sh text eol=lf
Dockerfile text eol=lf
**/Dockerfile text eol=lf
*.yml text eol=lf
**/*.yml text eol=lf
*.xml text eol=lf
**/*.xml text eol=lf
*.conf text eol=lf
**/*.conf text eol=lf

# Windows 管理脚本使用 CRLF，且只包含 ASCII 运行时文本以兼容 Windows PowerShell 5。
*.ps1 text eol=crlf
